# 4.1.4 Layout contract mobile:

- viewport dinamico con `dvh` e variabili `--safe-*`.
- modello `outer-scroll` sulle route core.
- tab bar integrata senza spacer artificiale.
- uso limitato/controllato di `vh/screen`.

